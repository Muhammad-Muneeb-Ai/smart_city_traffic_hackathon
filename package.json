{
  "name": "anpr-streamlit-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "echo 'Installing AI dependencies (this takes ~1 min)...' && pip install -r requirements.txt --no-cache-dir && echo 'Starting Streamlit Dashboard...' && python3 -m streamlit run app.py --server.port 3000 --server.address 0.0.0.0 --server.headless true",
    "build": "echo 'No build required for Streamlit'",
    "start": "python3 -m streamlit run app.py --server.port 3000 --server.address 0.0.0.0 --server.headless true"
  }
}
